<ul class="comments-index">
<% comments.each do |comment| %>
  <li>
    <p><%= comment.content %></p>
    <p>Posted by <%= User.find(comment.author_id).username %> @
      <%=comment.created_at %></p>
  </li>
<% end %>
</ul>